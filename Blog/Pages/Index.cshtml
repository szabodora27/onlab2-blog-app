@page
@model IndexModel

<h2 class="page-title">Üdvözlünk az Életmód blog olvasói közt</h2>
<hr />
<div class="container posts">
    <div class="row">
        @foreach (var post in Model.RecommendedPosts)
        {
            <div class="col-4 post-col">
                <div class="post-index-container">
                    <a asp-page="/ReadPost" asp-route-id="@post.Id">
                        <div class="post-index">
                            <div class="post-img" style="background-image:url('@post.FeaturedImageUrl')"></div>
                            <h5 class="post-title">@post.Title</h5>
                            <h6 class="created-by"><span>by </span>@post.CreatedBy</h6>
                        </div>
                    </a>
                </div>
            </div>
        }
    </div>
</div>
